---
phase: 07-mineru-feasibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/mineru/Dockerfile
  - tests/mineru/docker-compose.yml
  - tests/mineru/run-test.sh
  - tests/mineru/.gitignore
  - .planning/phases/07-mineru-feasibility/07-VALIDATION.md
autonomous: false
requirements:
  - MINERU-01
  - MINERU-02
  - MINERU-03
  - MINERU-04
user_setup:
  - task: "提供真实投标 PDF 测试文件"
    why: "MinerU 需要真实投标 PDF（扫描版+数字版）进行测试"
    instructions:
      - "将 1-2 个真实投标 PDF 文件放入 tests/mineru/fixtures/ 目录"
      - "至少包含一个数字版 PDF 和一个扫描版 PDF"
      - "文件名使用英文，如 digital-bid.pdf、scanned-bid.pdf"

must_haves:
  truths:
    - "MinerU 能在 Docker 中用 CPU 模式运行，处理真实投标 PDF"
    - "MinerU 输出的 content_list.json 结构已确认，type/text/table 等字段可用"
    - "中文投标文档的 OCR/文本提取质量已有 pdf-parse 对比数据"
    - "MinerU 依赖大小和 CPU 性能已有量化数据，有明确的可接受/不可接受判断"
  artifacts:
    - path: "tests/mineru/Dockerfile"
      provides: "CPU-only MinerU Docker 环境"
      contains: "FROM python:3.12-slim"
    - path: "tests/mineru/docker-compose.yml"
      provides: "一键启动 MinerU 测试容器"
      contains: "mineru"
    - path: ".planning/phases/07-mineru-feasibility/07-VALIDATION.md"
      provides: "完整验证报告"
      contains: "MINERU-01"
  key_links:
    - from: "tests/mineru/Dockerfile"
      to: "mineru[pipeline]"
      via: "pip install"
      pattern: "pip install.*mineru"
    - from: "tests/mineru/run-test.sh"
      to: "mineru CLI"
      via: "docker exec"
      pattern: "docker exec.*mineru"
---

<objective>
验证 MinerU 能否满足中文投标文档的 PDF 解析需求。

Purpose: 通过实际运行 MinerU 验证输出格式、中文解析质量、依赖大小和 CPU 性能，为后续集成决策提供数据支撑。
Output: 07-VALIDATION.md 包含所有 4 个 MINERU 需求的测试结果和结论。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mineru-feasibility/07-CONTEXT.md
@.planning/phases/07-mineru-feasibility/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 MinerU 测试基础设施</name>
  <files>tests/mineru/Dockerfile, tests/mineru/docker-compose.yml, tests/mineru/run-test.sh, tests/mineru/.gitignore</files>
  <read_first>
    - .planning/phases/07-mineru-feasibility/07-RESEARCH.md (Docker 部署方案、Common Pitfalls)
  </read_first>
  <action>
创建 `tests/mineru/` 目录，包含以下文件：

**Dockerfile** — CPU-only MinerU 环境：
```dockerfile
FROM python:3.12-slim

RUN apt-get update && \
    apt-get install -y fonts-noto-cjk fontconfig libgl1 && \
    fc-cache -fv && \
    apt-get clean

RUN pip install --no-cache-dir 'mineru[pipeline]>=3.4.0' \
    -i https://mirrors.aliyun.com/pypi/simple

ENV MINERU_MODEL_SOURCE=modelscope
RUN mineru-models-download -s modelscope -m pipeline

ENTRYPOINT ["mineru"]
```

**docker-compose.yml** — 一键启动：
```yaml
services:
  mineru:
    build: .
    volumes:
      - ./fixtures:/input:ro
      - ./output:/output
    shm_size: '4g'
    entrypoint: ["sleep", "infinity"]
```

**run-test.sh** — 测试脚本（在宿主机运行）：
```bash
#!/bin/bash
set -e
PDF_NAME="$1"
if [ -z "$PDF_NAME" ]; then
  echo "Usage: ./run-test.sh <pdf-filename>"
  echo "  PDF must be in tests/mineru/fixtures/"
  exit 1
fi

echo "=== Running MinerU on $PDF_NAME ==="
docker compose exec mineru mineru \
  -p "/input/$PDF_NAME" \
  -o "/output" \
  -b pipeline \
  -m auto \
  -l ch

echo "=== Output files ==="
ls -la "output/${PDF_NAME%.*}/"

echo "=== content_list.json preview (first 50 lines) ==="
head -50 "output/${PDF_NAME%.*}/${PDF_NAME%.*}_content_list.json"
```

**.gitignore**：
```
output/
fixtures/*.pdf
```

确保 `tests/mineru/fixtures/` 目录存在（空目录，放 .gitkeep）。
  </action>
  <verify>
    <automated>test -f tests/mineru/Dockerfile && test -f tests/mineru/docker-compose.yml && test -f tests/mineru/run-test.sh && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tests/mineru/Dockerfile contains "FROM python:3.12-slim"
    - tests/mineru/Dockerfile contains "pip install.*mineru[pipeline]"
    - tests/mineru/Dockerfile contains "MINERU_MODEL_SOURCE=modelscope"
    - tests/mineru/docker-compose.yml contains "mineru"
    - tests/mineru/docker-compose.yml contains "./fixtures:/input:ro"
    - tests/mineru/run-test.sh contains "mineru -p"
    - tests/mineru/run-test.sh contains "-b pipeline -m auto"
    - tests/mineru/.gitignore contains "output/"
  </acceptance_criteria>
  <done>MinerU 测试基础设施创建完成，包含 Dockerfile、docker-compose.yml、测试脚本和 .gitignore</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 构建 Docker 镜像并运行 MinerU 测试</name>
  <files>tests/mineru/output/</files>
  <read_first>
    - tests/mineru/Dockerfile
    - tests/mineru/docker-compose.yml
    - tests/mineru/run-test.sh
    - .planning/phases/07-mineru-feasibility/07-RESEARCH.md (Common Pitfalls, CLI 命令格式)
  </read_first>
  <what-built>
Task 1 已创建完整的 MinerU 测试基础设施（Dockerfile、docker-compose.yml、run-test.sh）。
现在需要构建 Docker 镜像、准备测试 PDF、运行 MinerU 并收集输出。
  </what-built>
  <action>
**Step 1: 准备测试 PDF**
将 1-2 个真实投标 PDF 放入 `tests/mineru/fixtures/` 目录：
- 至少一个数字版 PDF（如 digital-bid.pdf）
- 至少一个扫描版 PDF（如 scanned-bid.pdf）
- 文件名使用英文

**Step 2: 构建 Docker 镜像**
```bash
cd tests/mineru
docker compose build
```
首次构建会下载 MinerU 模型（约数分钟），耐心等待。

**Step 3: 启动容器**
```bash
docker compose up -d
```

**Step 4: 运行 MinerU 解析（每个 PDF 都跑）**
```bash
# 对数字版 PDF
docker compose exec mineru mineru \
  -p "/input/digital-bid.pdf" \
  -o "/output" \
  -b pipeline \
  -m auto \
  -l ch

# 对扫描版 PDF（如果有）
docker compose exec mineru mineru \
  -p "/input/scanned-bid.pdf" \
  -o "/output" \
  -b pipeline \
  -m auto \
  -l ch
```

**Step 5: 检查输出**
```bash
# 查看输出目录结构
ls -la tests/mineru/output/

# 查看 content_list.json 前 100 行
cat tests/mineru/output/<filename>/<filename>_content_list.json | head -100

# 统计 content_list.json 中各 type 的分布
cat tests/mineru/output/<filename>/<filename>_content_list.json | \
  python3 -c "import json,sys; data=json.load(sys.stdin); types={}; [types.update({b['type']: types.get(b['type'],0)+1}) for b in data]; print(types)"

# 查看 Markdown 输出
cat tests/mineru/output/<filename>/<filename>.md | head -50
```

**Step 6: 对比 pdf-parse 输出**
用现有 pdf-parse 解析同一 PDF，与 MinerU 输出对比文本提取质量：
```bash
# 查看现有 pdf-parse 输出（如果之前有保存）
# 否则手动对比 MinerU 的 .md 输出与 PDF 原文
```

**记录以下数据（后续 Task 3 需要）：**
- content_list.json 的实际 schema（字段名、值类型）
- 各 type 的分布（text/table/image/list 等数量）
- 中文文本提取质量（OCR 准确率、乱码情况）
- 表格识别质量（HTML table_body 是否正确）
- 与 pdf-parse 的差异（MinerU 多提取了什么、少了什么）
- 解析耗时（从 docker exec 开始到结束）
  </action>
  <resume-signal>MinerU 解析完成，输出已检查。请提供：1) content_list.json 的 type 分布；2) 中文解析质量评价；3) 解析耗时；4) 与 pdf-parse 的差异。</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 量化评估并编写验证报告</name>
  <files>.planning/phases/07-mineru-feasibility/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-mineru-feasibility/07-CONTEXT.md (性能基准、测试方法)
    - .planning/phases/07-mineru-feasibility/07-RESEARCH.md (content_list.json Schema, Common Pitfalls)
    - .planning/REQUIREMENTS.md (MINERU-01 到 MINERU-04 的具体要求)
  </read_first>
  <action>
创建 `.planning/phases/07-mineru-feasibility/07-VALIDATION.md`，包含以下章节：

```markdown
# Phase 7: MinerU 可行性验证报告

**测试日期:** YYYY-MM-DD
**MinerU 版本:** x.y.z
**测试环境:** Docker (python:3.12-slim), CPU-only, Windows 11

## 测试数据

| PDF 文件 | 类型 | 页数 | 说明 |
|----------|------|------|------|
| digital-bid.pdf | 数字版 | N 页 | ... |
| scanned-bid.pdf | 扫描版 | N 页 | ... |

## MINERU-01: 输出格式验证

### content_list.json Schema 确认

实际运行中观察到的字段：
- type: [列出实际出现的类型值]
- bbox: [确认格式]
- page_idx: [确认从 0 开始]
- text / text_level: [确认文本字段]
- table_body / table_caption: [确认表格字段]
- 其他字段: [...]

### content_list_v2.json 格式

[是否生成、格式差异]

### 输出目录结构

[实际输出的文件列表]

## MINERU-02: 中文解析质量

### 数字版 PDF

| 指标 | 结果 | 说明 |
|------|------|------|
| 文本提取准确率 | ... | 是否有乱码、漏字 |
| 表格识别 | ... | HTML table_body 是否正确 |
| 标题层级 | ... | text_level 是否准确 |
| 公章/印章 | ... | 是否识别为 image type |

### 扫描版 PDF

| 指标 | 结果 | 说明 |
|------|------|------|
| OCR 准确率 | ... | 中文字符识别准确度 |
| 表格识别 | ... | 扫描表格的识别效果 |
| 布局还原 | ... | 段落、标题的还原效果 |

### 与 pdf-parse 对比

| 维度 | pdf-parse | MinerU | 结论 |
|------|-----------|--------|------|
| 文本提取 | ... | ... | ... |
| 表格识别 | ... | ... | ... |
| 结构化程度 | ... | ... | ... |

## MINERU-03: 依赖大小评估

| 指标 | 数值 | 说明 |
|------|------|------|
| Docker 镜像大小 | ... GB | docker image ls |
| pip install 下载大小 | ... GB | pip install 数据 |
| 模型文件大小 | ... GB | 模型目录大小 |
| 总安装大小 | ... GB | 镜像+模型 |

**结论:** [可接受/需优化/不可接受]

## MINERU-04: CPU 性能评估

| PDF 文件 | 页数 | 解析耗时 | 秒/页 | 基准判断 |
|----------|------|----------|-------|----------|
| digital-bid.pdf | N | Ns | Ns/页 | <30s 可接受 |
| scanned-bid.pdf | N | Ns | Ns/页 | <30s 可接受 |

**性能基准:** <30秒/页可接受，>60秒不可接受
**结论:** [可接受/需优化/不可接受]

## 综合结论

### MinerU 可行性判断

| 维度 | 评分 | 说明 |
|------|------|------|
| 输出格式 | ✅/⚠️/❌ | ... |
| 中文解析质量 | ✅/⚠️/❌ | ... |
| 依赖大小 | ✅/⚠️/❌ | ... |
| CPU 性能 | ✅/⚠️/❌ | ... |

### 建议

[基于测试数据的明确建议：是否推荐集成 MinerU，以及集成路径]

### 已知限制

[测试中发现的问题和限制]
```

用 Task 2 收集的实际数据填充所有表格。对于无法自动获取的数据（如 Docker 镜像大小），运行以下命令补充：
```bash
# Docker 镜像大小
docker image ls mineru-mineru --format "{{.Size}}"

# 输出文件大小
du -sh tests/mineru/output/*/
```
  </action>
  <verify>
    <automated>test -f .planning/phases/07-mineru-feasibility/07-VALIDATION.md && grep -q "MINERU-01" .planning/phases/07-mineru-feasibility/07-VALIDATION.md && grep -q "MINERU-02" .planning/phases/07-mineru-feasibility/07-VALIDATION.md && grep -q "MINERU-03" .planning/phases/07-mineru-feasibility/07-VALIDATION.md && grep -q "MINERU-04" .planning/phases/07-mineru-feasibility/07-VALIDATION.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/07-mineru-feasibility/07-VALIDATION.md 文件存在
    - 文件包含 "MINERU-01" 章节，有 content_list.json schema 确认
    - 文件包含 "MINERU-02" 章节，有中文解析质量数据
    - 文件包含 "MINERU-03" 章节，有依赖大小量化数据
    - 文件包含 "MINERU-04" 章节，有 CPU 性能数据和基准判断
    - 文件包含 "综合结论" 章节，有明确的可行性判断
    - 所有数值表格有实际数据（非占位符）
  </acceptance_criteria>
  <done>07-VALIDATION.md 完成，包含所有 4 个 MINERU 需求的测试结果、量化数据和可行性结论</done>
</task>

</tasks>

<verification>
Phase 7 验证：
- tests/mineru/Dockerfile 存在，配置 CPU-only MinerU 环境
- tests/mineru/docker-compose.yml 存在，配置卷挂载和共享内存
- tests/mineru/run-test.sh 存在，可一键运行 MinerU 测试
- .planning/phases/07-mineru-feasibility/07-VALIDATION.md 存在，包含 MINERU-01 到 MINERU-04 全部测试结果
</verification>

<success_criteria>
1. MinerU Docker 镜像可成功构建，CPU 模式可运行
2. MinerU 能处理真实投标 PDF，输出 content_list.json
3. 07-VALIDATION.md 包含输出格式、中文质量、依赖大小、CPU 性能四维度的量化数据
4. 有明确的可行性结论和集成建议
</success_criteria>

<output>
After completion, create `.planning/phases/07-mineru-feasibility/07-01-SUMMARY.md`
</output>
