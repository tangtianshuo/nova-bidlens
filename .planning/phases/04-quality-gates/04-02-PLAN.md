---
phase: 04-quality-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/performance/sparse-recall.test.ts
  - tests/performance/findings-rendering.test.ts
  - tests/regression/diff-evidence.test.ts
autonomous: true
requirements: [QA-04, QA-05]

must_haves:
  truths:
    - "Performance tests handle sparse recall on 4000-page document ASTs"
    - "Store filters 1000+ findings within 100ms"
    - "Risk pipeline produces evidence compatible with V0.2.2 DiffItem format"
    - "Export model from risk pipeline matches expected structure"
  artifacts:
    - path: "tests/performance/sparse-recall.test.ts"
      provides: "Sparse recall performance tests on large document ASTs"
    - path: "tests/performance/findings-rendering.test.ts"
      provides: "1000+ findings store filtering performance tests"
    - path: "tests/regression/diff-evidence.test.ts"
      provides: "Diff evidence compatibility regression tests"
  key_links:
    - from: "tests/performance/sparse-recall.test.ts"
      to: "tests/benchmark/benchmark-harness.ts"
      via: "import BenchmarkRunner for timing"
    - from: "tests/regression/diff-evidence.test.ts"
      to: "packages/shared/src/risk-review.ts"
      via: "import RiskFinding and Evidence types"
---

<objective>
Add performance tests (sparse recall on 4000-page documents, 1000+ findings rendering) and Diff regression tests for evidence compatibility.

Purpose: Verify the risk pipeline handles large documents and findings without degradation, and that evidence output is compatible with existing Diff tooling.
Output: Performance benchmark tests + diff regression test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/benchmark/benchmark-harness.ts
@tests/benchmark/performance.test.ts
@apps/desktop/src/renderer/features/risk-review/risk-review-performance.test.tsx
@apps/desktop/src/renderer/features/risk-review/risk-review-store.ts
@apps/desktop/src/renderer/__fixtures__/risk-project.ts
@packages/shared/src/risk-review.ts
@packages/shared/src/ipc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Performance tests — sparse recall and findings rendering</name>
  <files>tests/performance/sparse-recall.test.ts, tests/performance/findings-rendering.test.ts</files>
  <read_first>
    - tests/benchmark/benchmark-harness.ts (BenchmarkRunner API — run, phases, compareWithBaseline)
    - tests/benchmark/performance.test.ts (existing perf test patterns — fixture generators, budget assertions)
    - apps/desktop/src/renderer/features/risk-review/risk-review-performance.test.tsx (existing 1000-findings test pattern)
    - apps/desktop/src/renderer/features/risk-review/risk-review-store.ts (matchesFilter, useRiskReviewStore)
    - apps/desktop/src/renderer/__fixtures__/risk-project.ts (buildReadyScenario, RiskFinding shape)
    - packages/shared/src/risk-review.ts (RiskFinding, Evidence, ScoreBreakdown types)
  </read_first>
  <action>
Create two performance test files:

**File 1: `tests/performance/sparse-recall.test.ts`**
Tests sparse recall on large document ASTs (simulating 4000-page documents).

- Generate a large DocumentAst with 4000 sections (each section = ~1 page equivalent)
  - Use the pattern from `tests/benchmark/performance.test.ts` generateLargeDocxXml
  - Each section has 3-5 paragraphs with realistic Chinese text
  - Total blocks: ~16000-20000 paragraph blocks
- Test: `it('generates 4000-page document AST within 5s')` — use BenchmarkRunner to time the generation
- Test: `it('filters large document AST by section within 500ms')` — filter blocks by type='section'
- Test: `it('serializes large document AST to JSON within 2s')` — JSON.stringify the full AST
- Test: `it('deserializes large document AST from JSON within 2s')` — JSON.parse back

Use BenchmarkRunner from `tests/benchmark/benchmark-harness.ts` for timing.

**File 2: `tests/performance/findings-rendering.test.ts`**
Tests store filtering and sorting with 1000+ findings.

- Generate 1000+ RiskFinding objects using the makeFinding pattern from `risk-review-performance.test.tsx`
  - Use DEFAULT_SCORE shape: `{ exactMatchScore, lexicalScore, structuralScore, entityScore, factScore, tenderDiscount, templateDiscount, factConflictPenalty, finalScore, ruleVersion }`
  - Vary riskLevel (high/medium/low), detectorType (text/table/entity), reviewStatus (pending/confirmed/dismissed)
- Test: `it('filters 1000 findings by risk level in under 50ms')` — filter by Set(['high'])
- Test: `it('filters 1000 findings by detector type in under 50ms')` — filter by Set(['text'])
- Test: `it('filters 1000 findings by review status in under 50ms')` — filter by Set(['confirmed'])
- Test: `it('filters 1000 findings by search text in under 100ms')` — search by substring
- Test: `it('sorts 1000 findings by confidence in under 100ms')` — sort descending
- Test: `it('combined filter on 1000 findings in under 100ms')` — risk level + detector type + search text

Import `matchesFilter` from `apps/desktop/src/renderer/features/risk-review/risk-review-store.ts` for the filtering logic. If the import path doesn't work in vitest (it's a renderer module), replicate the filter logic inline — it's a simple predicate function.

Budget thresholds: 50ms for single filters, 100ms for combined/sort, 500ms for AST operations, 2-5s for large AST generation.
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-bidlens && npx vitest run tests/performance/</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/performance/sparse-recall.test.ts` exists with at least 4 test cases
    - File `tests/performance/findings-rendering.test.ts` exists with at least 6 test cases
    - All performance tests pass within their budget thresholds
    - `it('filters 1000 findings by risk level in under 50ms')` completes in < 50ms
    - `it('generates 4000-page document AST within 5s')` completes in < 5000ms
    - Tests use BenchmarkRunner from tests/benchmark/benchmark-harness.ts for timing
    - All tests pass when run via `npx vitest run tests/performance/`
  </acceptance_criteria>
  <done>Performance tests verify sparse recall on 4000-page documents and 1000+ findings filtering/sorting within budget</done>
</task>

<task type="auto">
  <name>Task 2: Diff regression tests — evidence compatibility</name>
  <files>tests/regression/diff-evidence.test.ts</files>
  <read_first>
    - packages/shared/src/risk-review.ts (RiskFinding, Evidence, ScoreBreakdown, FilePairAssessment types)
    - packages/shared/src/ipc.ts (ExportRiskReportRequest/Response types)
    - tests/e2e/risk-pipeline.test.ts (existing E2E evidence verification patterns)
    - tests/e2e/v022-workflow.test.ts (V0.2.2 DiffItem structure — matchType, confidence, similarity, diffDetail)
    - apps/desktop/src/renderer/__fixtures__/risk-project.ts (buildReadyScenario — full RiskFinding fixture)
  </read_first>
  <action>
Create `tests/regression/diff-evidence.test.ts` with regression tests verifying risk pipeline evidence is compatible with V0.2.2 Diff tooling:

**1. Evidence structure compatibility**
- Import RiskFinding and Evidence types from `@bidlens/shared`
- Build a RiskFinding fixture (use buildReadyScenario or inline construction)
- Test: `it('finding evidence has required fields')` — each Evidence has sourceSubmissionId, targetSubmissionId, sourceNodeId, targetNodeId, sourceOriginalText, targetOriginalText, similarityScore, sourceSectionPath
- Test: `it('evidence similarityScore is between 0 and 1')` — clamp check
- Test: `it('evidence sourceSectionPath is a non-empty array')` — array type check

**2. ScoreBreakdown structure**
- Test: `it('ScoreBreakdown has all required fields')` — exactMatchScore, lexicalScore, structuralScore, entityScore, factScore, tenderDiscount, templateDiscount, factConflictPenalty, finalScore, ruleVersion
- Test: `it('finalScore is sum of component scores')` — verify the math: finalScore = exactMatchScore + lexicalScore + structuralScore + entityScore + factScore - tenderDiscount - templateDiscount - factConflictPenalty (approximately, allow rounding)

**3. FilePairAssessment compatibility**
- Build a FilePairAssessment fixture
- Test: `it('file pair assessment has source/target submission IDs')` — fromSubmissionId, toSubmissionId
- Test: `it('file pair assessment has risk level and score')` — riskLevel in ['low','medium','high'], overallScore between 0 and 1

**4. Project-level assessment**
- Test: `it('project assessment has risk level')` — riskLevel field exists and is valid enum value
- Test: `it('project assessment has summary text')` — summary is a non-empty string

**5. Export model compatibility**
- Build an ExportRiskReportRequest fixture
- Test: `it('export request has valid format')` — format in ['pdf', 'html', 'markdown']
- Test: `it('export request has valid scope')` — scope in ['all', 'confirmed', 'important', 'filtered']

**6. V0.2.2 DiffItem field presence in RiskFinding**
- The V0.2.2 DiffItem has: matchId, matchType, confidence, similarity, sourceA, sourceB, nodeIdsA, nodeIdsB, diffDetail, summary
- RiskFinding has: id (≈matchId), detectorType (≈matchType category), confidenceScore (≈confidence), symmetricSimilarity (≈similarity), evidence[].sourceOriginalText (≈sourceA), evidence[].targetOriginalText (≈sourceB)
- Test: `it('RiskFinding fields map to DiffItem equivalents')` — verify the structural mapping exists (not that they're identical, but that the data is present for migration/compatibility)
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-bidlens && npx vitest run tests/regression/diff-evidence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/regression/diff-evidence.test.ts` exists with at least 10 test cases
    - `it('finding evidence has required fields')` passes — all 8 Evidence fields present
    - `it('ScoreBreakdown has all required fields')` passes — all 10 fields present
    - `it('file pair assessment has source/target submission IDs')` passes
    - `it('export request has valid format')` passes — pdf/html/markdown
    - `it('RiskFinding fields map to DiffItem equivalents')` passes
    - All tests pass when run via `npx vitest run tests/regression/diff-evidence.test.ts`
  </acceptance_criteria>
  <done>Diff regression tests verify evidence structure, score breakdown, assessment format, and V0.2.2 compatibility</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/performance/` — all performance tests pass within budgets
- `npx vitest run tests/regression/diff-evidence.test.ts` — all regression tests pass
- `npx vitest run` — full test suite still passes (no regressions)
</verification>

<success_criteria>
- Performance tests verify 4000-page document handling and 1000+ findings filtering within budgets
- Diff regression tests verify evidence structure compatibility with V0.2.2 tooling
- All tests pass without modifying production code
- Performance tests use BenchmarkRunner for consistent timing
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-gates/04-02-SUMMARY.md`
</output>
