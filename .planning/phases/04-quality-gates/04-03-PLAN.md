---
phase: 04-quality-gates
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - apps/desktop/tests/e2e/viewport-screenshots.test.ts
autonomous: false
requirements: [QA-06]

must_haves:
  truths:
    - "Risk result page renders correctly at 1280x800 viewport"
    - "Risk result page renders correctly at 1024x700 viewport"
    - "Risk result page renders correctly at 760px equivalent viewport"
    - "Key UI elements have ARIA labels for accessibility"
    - "Screenshots are captured at each viewport width"
  artifacts:
    - path: "apps/desktop/tests/e2e/viewport-screenshots.test.ts"
      provides: "Playwright viewport and accessibility screenshot tests"
    - path: "apps/desktop/tests/e2e/screenshots/"
      provides: "Captured screenshot PNG files at each viewport width"
  key_links:
    - from: "apps/desktop/tests/e2e/viewport-screenshots.test.ts"
      to: "apps/desktop/tests/e2e/setup.ts"
      via: "import launchTestApp for Electron test context"
    - from: "apps/desktop/tests/e2e/viewport-screenshots.test.ts"
      to: "apps/desktop/tests/e2e/risk-pipeline.test.ts"
      via: "follow same pattern: launch, create project, wait for findings, screenshot"
---

<objective>
Capture viewport/accessibility screenshots at 1280x800, 1024x700, and 760px equivalent widths for the risk review UI.

Purpose: Verify the UI is usable and accessible at different viewport sizes, and capture baseline screenshots for visual regression.
Output: Playwright test that launches Electron, processes a project, and screenshots the risk result page at 3 viewport widths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/desktop/tests/e2e/setup.ts
@apps/desktop/tests/e2e/helpers.ts
@apps/desktop/tests/e2e/risk-pipeline.test.ts
@apps/desktop/tests/e2e/fixtures/create-docx.ts
@apps/desktop/playwright.config.ts
@apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx
@apps/desktop/src/renderer/features/risk-review/finding-virtual-list.tsx
@apps/desktop/src/renderer/features/risk-review/evidence-viewport.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright viewport and accessibility screenshot tests</name>
  <files>apps/desktop/tests/e2e/viewport-screenshots.test.ts</files>
  <read_first>
    - apps/desktop/tests/e2e/setup.ts (launchTestApp, TestContext, cleanupDir)
    - apps/desktop/tests/e2e/helpers.ts (createTestProject, waitForStatus, getProjectDetail, waitForFindings)
    - apps/desktop/tests/e2e/risk-pipeline.test.ts (full pipeline pattern — beforeAll creates project, waits for findings)
    - apps/desktop/tests/e2e/fixtures/create-docx.ts (createSimilarDocs for DOCX fixtures)
    - apps/desktop/playwright.config.ts (testDir, timeout, workers config)
    - apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx (component structure — onBack prop, FindingFilterToolbar, FindingVirtualList, EvidenceViewport, RelationshipMatrix)
    - apps/desktop/src/renderer/features/risk-review/finding-virtual-list.tsx (listbox with aria-label '发现项列表')
  </read_first>
  <action>
Create `apps/desktop/tests/e2e/viewport-screenshots.test.ts` as a Playwright test that:

**Setup (beforeAll):**
- Import `launchTestApp`, `cleanupDir` from `./setup`
- Import `createTestProject`, `waitForStatus`, `getProjectDetail`, `waitForFindings` from `./helpers`
- Import `createSimilarDocs` from `./fixtures/create-docx`
- Launch Electron app via `launchTestApp()`
- Create DOCX fixtures via `createSimilarDocs(ctx.exportDir)`
- Create project via `createTestProject(ctx.page, fixturePaths)`
- Wait for findings via `waitForFindings(ctx.page, projectId, 120_000)`
- Ensure screenshots directory exists: `apps/desktop/tests/e2e/screenshots/`

**Test: 1280x800 viewport**
- Set viewport: `ctx.page.setViewportSize({ width: 1280, height: 800 })`
- Navigate to risk result page: after waitForFindings, the app should show the result page
  - If not auto-navigated, click the "查看结果" button or similar UI element
  - Or use `ctx.page.evaluate(() => { (window as any).__ZUSTAND_STORE?.setView?.('risk-result'); })` if store is exposed
- Wait for UI to stabilize: `await ctx.page.waitForTimeout(1000)`
- Screenshot: `await ctx.page.screenshot({ path: 'apps/desktop/tests/e2e/screenshots/viewport-1280x800.png', fullPage: true })`
- ARIA check: verify `ctx.page.locator('[aria-label]')` returns at least 3 elements
- ARIA check: verify finding list has `role="listbox"` with `aria-label`

**Test: 1024x700 viewport**
- Set viewport: `ctx.page.setViewportSize({ width: 1024, height: 700 })`
- Wait for reflow: `await ctx.page.waitForTimeout(500)`
- Screenshot: `await ctx.page.screenshot({ path: 'apps/desktop/tests/e2e/screenshots/viewport-1024x700.png', fullPage: true })`
- ARIA check: key interactive elements still have accessible names

**Test: 760px viewport (equivalent to 760 width)**
- Set viewport: `ctx.page.setViewportSize({ width: 760, height: 1024 })`
- Wait for reflow: `await ctx.page.waitForTimeout(500)`
- Screenshot: `await ctx.page.screenshot({ path: 'apps/desktop/tests/e2e/screenshots/viewport-760.png', fullPage: true })`
- ARIA check: verify no elements are visually hidden but still accessible (basic check)

**Cleanup (afterAll):**
- Close Electron app
- Cleanup temp directories

**Important:** This test uses the existing `apps/desktop/playwright.config.ts` (testDir is `./tests/e2e`), so it will be discovered automatically when running `npx playwright test` from the `apps/desktop` directory.
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-bidlens/apps/desktop && npx playwright test tests/e2e/viewport-screenshots.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/desktop/tests/e2e/viewport-screenshots.test.ts` exists
    - Test contains 3 viewport test cases: 1280x800, 1024x700, 760x1024
    - Each test captures a screenshot PNG file
    - At least one test verifies ARIA labels exist on key elements
    - Screenshot files exist at `apps/desktop/tests/e2e/screenshots/viewport-*.png` after test run
    - Tests pass when run via `npx playwright test tests/e2e/viewport-screenshots.test.ts`
  </acceptance_criteria>
  <done>Viewport screenshots captured at 1280x800, 1024x700, and 760px widths with ARIA accessibility verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify viewport screenshots and accessibility</name>
  <files>apps/desktop/tests/e2e/screenshots/</files>
  <action>
Human reviews captured screenshots and verifies UI usability at each viewport width.
  </action>
  <what-built>Viewport and accessibility screenshot tests that capture the risk review UI at 3 viewport widths (1280x800, 1024x700, 760px) and verify ARIA labels exist.</what-built>
  <how-to-verify>
    1. Review the captured screenshots at `apps/desktop/tests/e2e/screenshots/viewport-*.png`
    2. Verify the UI is usable at each viewport width:
       - 1280x800: Full layout with sidebar, findings list, and evidence viewport visible
       - 1024x700: Compact but functional layout
       - 760px: Responsive layout adapts (may collapse sidebar)
    3. Verify ARIA labels are present on interactive elements
    4. Run `npx playwright test tests/e2e/viewport-screenshots.test.ts` to confirm all tests pass
  </how-to-verify>
  <verify>
    <automated>ls D:/Projects/Nova/nova-bidlens/apps/desktop/tests/e2e/screenshots/viewport-*.png</automated>
  </verify>
  <resume-signal>Type "approved" if screenshots look correct and tests pass, or describe issues to fix.</resume-signal>
  <done>User confirms screenshots are acceptable and UI is usable at all 3 viewport widths</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/e2e/viewport-screenshots.test.ts` — all viewport tests pass
- Screenshot PNG files exist at `apps/desktop/tests/e2e/screenshots/`
- ARIA labels verified on key UI elements
</verification>

<success_criteria>
- Screenshots captured at all 3 viewport widths
- UI renders correctly at each viewport (no broken layouts, overlapping elements)
- ARIA labels present on finding list, evidence viewport, and interactive controls
- User verifies screenshots are acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-gates/04-03-SUMMARY.md`
</output>
