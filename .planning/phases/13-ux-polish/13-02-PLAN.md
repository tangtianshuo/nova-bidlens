---
phase: 13-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - apps/desktop/src/main/services/parser-service.ts
  - apps/desktop/src/main/services/risk-review-service.ts
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "MinerU parsing emits periodic progress updates with elapsed seconds via risk:progress IPC"
    - "UI shows 'MinerU 解析中 (已等待 Xs)' during cloud parsing"
    - "Progress timer is cleaned up when parsing completes or is cancelled"
  artifacts:
    - path: "apps/desktop/src/main/services/parser-service.ts"
      provides: "onProgress callback in ParserServiceOptions, periodic timer in parseWithMinerU"
      contains: "onProgress"
    - path: "apps/desktop/src/main/services/risk-review-service.ts"
      provides: "onProgress callback passed to parseDocumentFile"
      contains: "onProgress"
---

<objective>
Add real-time progress feedback during MinerU cloud parsing. Currently the UI shows a static "正在解析投标文件..." while MinerU polls for 1-3 minutes. Add periodic progress updates showing elapsed seconds.

Purpose: UX-01 — "MinerU 解析期间显示进度反馈 — 通过 risk:progress IPC 推送 'MinerU 解析中 (已等待 Xs)' 到 UI"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/desktop/src/main/services/parser-service.ts
@apps/desktop/src/main/services/risk-review-service.ts
@packages/shared/src/ipc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onProgress callback to ParserServiceOptions and wire into parseWithMinerU</name>
  <files>apps/desktop/src/main/services/parser-service.ts</files>
  <read_first>
    - apps/desktop/src/main/services/parser-service.ts
  </read_first>
  <action>
Add `onProgress?: (stageLabel: string) => void` to `ParserServiceOptions`. In `parseWithMinerU`, start a 1-second interval timer before calling `mineru.parse()` that calls `onProgress` with `"MinerU 解析中 (已等待 Xs)"`. Clear the timer in a finally block. Pass `opts` through to `parseWithMinerU` from `parseDocumentFile`.
  </action>
  <verify>
    <automated>grep -c "onProgress" apps/desktop/src/main/services/parser-service.ts</automated>
  </verify>
  <acceptance_criteria>
    - ParserServiceOptions has onProgress callback
    - parseWithMinerU accepts opts and starts/clears interval timer
    - Timer calls onProgress with elapsed seconds every second
    - parseDocumentFile passes opts to parseWithMinerU
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire onProgress callback from risk-review-service.ts</name>
  <files>apps/desktop/src/main/services/risk-review-service.ts</files>
  <read_first>
    - apps/desktop/src/main/services/risk-review-service.ts
  </read_first>
  <action>
In the parsing loop of `run()`, pass `onProgress` callback to `parseDocumentFile` that calls `this.emitProgress()` with the stage label from the parser. This replaces the static "正在解析投标文件..." with live "MinerU 解析中 (已等待 Xs)" during cloud parsing.
  </action>
  <verify>
    <automated>grep -c "onProgress" apps/desktop/src/main/services/risk-review-service.ts</automated>
  </verify>
  <acceptance_criteria>
    - parseDocumentFile calls receive onProgress callback
    - Callback emits risk:progress with stageLabel from parser
    - Progress updates flow to UI during MinerU parsing
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep -c "onProgress" apps/desktop/src/main/services/parser-service.ts` >= 3
2. `grep -c "onProgress" apps/desktop/src/main/services/risk-review-service.ts` >= 2
3. TypeScript compiles: `pnpm --filter @bidlens/shared build && pnpm --filter @bidlens/desktop build`
</verification>

<success_criteria>
- MinerU parsing emits periodic progress updates with elapsed seconds
- UI shows "MinerU 解析中 (已等待 Xs)" during cloud parsing
- Progress timer is cleaned up on completion/cancellation
- No memory leaks from uncleared intervals
</success_criteria>

<output>
After completion, create `.planning/phases/13-ux-polish/13-02-SUMMARY.md`
</output>
