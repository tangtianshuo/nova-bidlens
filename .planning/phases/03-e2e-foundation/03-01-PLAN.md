---
phase: 03-e2e-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/index.ts
  - apps/desktop/playwright.config.ts
  - apps/desktop/tests/e2e/setup.ts
  - apps/desktop/tests/e2e/smoke.test.ts
  - apps/desktop/package.json
autonomous: true
requirements: [QA-01]

must_haves:
  truths:
    - "E2E test app launches with isolated userData directory (no shared DB)"
    - "window.bidlens API is fully exposed and responds to IPC calls"
    - "Smoke tests pass against the built Electron app"
    - "Test cleanup removes temp directories after run"
  artifacts:
    - path: "apps/desktop/src/main/index.ts"
      provides: "BIDLENS_TEST_DATA_DIR env var override for test isolation"
      contains: "BIDLENS_TEST_DATA_DIR"
    - path: "apps/desktop/playwright.config.ts"
      provides: "Playwright config with build step"
      contains: "webServer"
    - path: "apps/desktop/tests/e2e/setup.ts"
      provides: "Test harness with DB path access"
    - path: "apps/desktop/tests/e2e/smoke.test.ts"
      provides: "Smoke tests covering launch, IPC, and DB isolation"
  key_links:
    - from: "apps/desktop/src/main/index.ts"
      to: "PersistenceManager"
      via: "env var override passed to constructor"
      pattern: "BIDLENS_TEST_DATA_DIR"
    - from: "apps/desktop/playwright.config.ts"
      to: "apps/desktop/tests/e2e"
      via: "testDir config"
      pattern: "testDir"
---

<objective>
Harden the Playwright E2E test harness so it can reliably launch the Electron app with isolated state and exercise IPC.

Purpose: The existing E2E scaffold (setup.ts, helpers.ts, smoke.test.ts) exists but the main process ignores BIDLENS_TEST_DATA_DIR, so tests share the real user DB. This plan wires test isolation and ensures the harness works end-to-end.

Output: Working E2E harness that builds the app, launches with isolated DB, and passes smoke tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-e2e-foundation/03-CONTEXT.md

@apps/desktop/src/main/index.ts
@apps/desktop/src/main/services/persistence.ts
@apps/desktop/playwright.config.ts
@apps/desktop/tests/e2e/setup.ts
@apps/desktop/tests/e2e/helpers.ts
@apps/desktop/tests/e2e/smoke.test.ts
@apps/desktop/package.json
@apps/desktop/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire BIDLENS_TEST_DATA_DIR in main process and add E2E build script</name>
  <files>apps/desktop/src/main/index.ts, apps/desktop/package.json, apps/desktop/playwright.config.ts</files>
  <read_first>
    - apps/desktop/src/main/index.ts
    - apps/desktop/src/main/services/persistence.ts
    - apps/desktop/package.json
    - apps/desktop/playwright.config.ts
  </read_first>
  <action>
1. In `apps/desktop/src/main/index.ts`:
   - Before `persistence = new PersistenceManager()`, check for `process.env.BIDLENS_TEST_DATA_DIR`:
     ```typescript
     const testDataDir = process.env.BIDLENS_TEST_DATA_DIR || undefined;
     persistence = new PersistenceManager(testDataDir);
     ```
   - The PersistenceManager constructor already accepts `dataDir?: string` and passes it to DatabaseManager. No changes needed in persistence.ts.

2. In `apps/desktop/package.json`, add a script that builds TS then runs Playwright:
   ```json
   "test:e2e": "tsc -p tsconfig.main.json && playwright test"
   ```
   Update the existing `test:e2e` script (currently just `playwright test`) to include the build step.

3. In `apps/desktop/playwright.config.ts`, add a `webServer` config to auto-build before tests:
   Actually, since this is Electron (not a web server), skip webServer. Instead rely on the `test:e2e` npm script build step. No changes needed to playwright.config.ts for this.
  </action>
  <verify>
    <automated>cd apps/desktop && grep -n "BIDLENS_TEST_DATA_DIR" src/main/index.ts && grep "test:e2e" package.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/main/index.ts contains `process.env.BIDLENS_TEST_DATA_DIR`
    - PersistenceManager receives testDataDir when env var is set
    - package.json test:e2e script includes tsc build step
  </acceptance_criteria>
  <done>Main process respects BIDLENS_TEST_DATA_DIR for test isolation; E2E script builds TS before running Playwright</done>
</task>

<task type="auto">
  <name>Task 2: Harden smoke tests and add DB isolation verification</name>
  <files>apps/desktop/tests/e2e/setup.ts, apps/desktop/tests/e2e/smoke.test.ts</files>
  <read_first>
    - apps/desktop/tests/e2e/setup.ts
    - apps/desktop/tests/e2e/smoke.test.ts
    - apps/desktop/tests/e2e/helpers.ts
    - apps/desktop/src/main/db/database.ts
  </read_first>
  <action>
1. In `apps/desktop/tests/e2e/setup.ts`:
   - Add a helper `getDbPath(context: TestContext): string` that returns `context.dbPath`
   - Add a helper `verifyDbExists(context: TestContext): boolean` that checks `fs.existsSync(context.dbPath)`
   - Export these for use in tests

2. In `apps/desktop/tests/e2e/smoke.test.ts`:
   - Add a test "DB is created in isolated directory" that verifies:
     - `ctx.dbPath` exists after app launch
     - The DB file is inside the temp `userDataDir`, not the real user data dir
   - Add a test "project list returns empty array for fresh DB" that verifies:
     - `listProjects()` returns `[]` on a fresh isolated DB
   - Strengthen the existing "can create a risk project via IPC" test:
     - Still expects failure (files don't exist), but verify the error is about file validation, not about missing IPC handler
   - Ensure afterAll cleanup is robust (already exists, just verify it runs)
  </action>
  <verify>
    <automated>cd apps/desktop && npx tsc -p tsconfig.main.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - setup.ts exports verifyDbExists helper
    - smoke.test.ts has "DB is created in isolated directory" test
    - smoke.test.ts has "project list returns empty array for fresh DB" test
    - All smoke tests are syntactically valid TypeScript
  </acceptance_criteria>
  <done>Smoke tests verify DB isolation and IPC round-trip; test harness is ready for full pipeline E2E tests</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd apps/desktop && npx tsc -p tsconfig.main.json --noEmit`
2. Smoke test file is syntactically valid: `cd apps/desktop && npx tsc --noEmit tests/e2e/smoke.test.ts` (may need path alias resolution — skip if it fails on import paths, the real check is Playwright execution)
3. Key pattern checks:
   - `grep -n "BIDLENS_TEST_DATA_DIR" apps/desktop/src/main/index.ts` returns match
   - `grep "test:e2e" apps/desktop/package.json` includes tsc build
</verification>

<success_criteria>
- Main process creates isolated DB when BIDLENS_TEST_DATA_DIR is set
- E2E build script compiles TS before running Playwright
- Smoke tests verify app launch, IPC exposure, DB isolation, and project list
- All changes compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2e-foundation/03-01-SUMMARY.md`
</output>
