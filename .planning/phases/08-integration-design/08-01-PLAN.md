---
phase: 08-integration-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/features/projects/submission-file-list.tsx
autonomous: true
requirements:
  - "D-04"

must_haves:
  truths:
    - "用户可以拖放或选择 .nzbtf 文件添加到投标文件列表"
    - "nZBTF 文件在上传区域显示为 NZBTF 格式标签"
  artifacts:
    - path: "apps/desktop/src/renderer/features/projects/submission-file-list.tsx"
      provides: "addFilesFromList accepts .nzbtf extension"
      contains: "nzbtf"
  key_links:
    - from: "addFilesFromList"
      to: "file.name.split('.').pop()"
      via: "extension check"
      pattern: "ext !== 'docx' && ext !== 'pdf'"
---

<objective>
修复 nZBTF 文件上传过滤 bug。

Purpose: `addFilesFromList` 函数第 199 行只接受 `.docx` 和 `.pdf`，导致 `.nzbtf` 文件被静默忽略。
Output: 修复后的 submission-file-list.tsx，支持 .nzbtf 文件上传。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-design/08-CONTEXT.md

@apps/desktop/src/renderer/features/projects/submission-file-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix addFilesFromList to accept .nzbtf extension</name>
  <files>apps/desktop/src/renderer/features/projects/submission-file-list.tsx</files>
  <read_first>
    - apps/desktop/src/renderer/features/projects/submission-file-list.tsx
  </read_first>
  <action>
Per D-04: Fix the extension filter in `addFilesFromList` (line 199).

Current code:
```typescript
if (ext !== 'docx' && ext !== 'pdf') continue;
```

Change to:
```typescript
if (ext !== 'docx' && ext !== 'pdf' && ext !== 'nzbtf') continue;
```

This is the only change needed. The rest of the file already supports nZBTF:
- `ALLOWED_FORMATS` already includes `'nzbtf'` (line 42)
- `SubmissionFile.format` type already includes `'nzbtf'` (line 13)
- `<input accept>` already includes `.nzbtf` (line 310)
- Upload hint text already mentions `.nzbtf` (line 276)
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-bidlens && grep -n "nzbtf" apps/desktop/src/renderer/features/projects/submission-file-list.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/renderer/features/projects/submission-file-list.tsx line 199 contains `ext !== 'nzbtf'`
    - grep for `ext !== 'docx' && ext !== 'pdf' && ext !== 'nzbtf'` matches exactly 1 line in the file
    - No other lines in addFilesFromList were modified
  </acceptance_criteria>
  <done>addFilesFromList accepts .nzbtf files alongside .docx and .pdf</done>
</task>

</tasks>

<verification>
1. File compiles: `pnpm --filter @bidlens/desktop build` succeeds
2. nZBTF extension accepted: grep confirms filter includes 'nzbtf'
</verification>

<success_criteria>
用户可以拖放或选择 .nzbtf 文件，文件列表正确显示 NZBTF 格式标签。
</success_criteria>

<output>
After completion, create `.planning/phases/08-integration-design/08-01-SUMMARY.md`
</output>
