---
phase: 02-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/features/projects/project-processing-page.tsx
  - apps/desktop/src/renderer/features/projects/project-processing-page.test.tsx
  - apps/desktop/src/renderer/app/App.tsx
  - apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx
autonomous: true
requirements:
  - HARDEN-01
  - HARDEN-04

must_haves:
  truths:
    - "ProjectProcessingPage reads projectId from useRiskReviewStore, not useProjectStore"
    - "RiskResultPage calls all hooks before any early returns (no conditional hook calls)"
    - "App.tsx does not set useAppStore.taskId for the risk-review flow"
    - "Back button on processing page clears useRiskReviewStore.projectId"
  artifacts:
    - path: "apps/desktop/src/renderer/features/projects/project-processing-page.tsx"
      provides: "Processing page reading from correct store"
      contains: "useRiskReviewStore"
    - path: "apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx"
      provides: "Hook ordering fix"
      contains: "useFindingCounts(project?.findings ?? EMPTY_FINDINGS)"
    - path: "apps/desktop/src/renderer/app/App.tsx"
      provides: "Clean startRiskProject without taskId"
      contains: "useRiskReviewStore.getState().setProjectId(projectId)"
  key_links:
    - from: "apps/desktop/src/renderer/features/projects/project-processing-page.tsx"
      to: "apps/desktop/src/renderer/features/risk-review/risk-review-store.ts"
      via: "useRiskReviewStore import and projectId read"
      pattern: "useRiskReviewStore.*projectId"
    - from: "apps/desktop/src/renderer/app/App.tsx"
      to: "apps/desktop/src/renderer/features/risk-review/risk-review-store.ts"
      via: "setProjectId on create and open"
      pattern: "useRiskReviewStore.getState\\(\\).setProjectId"
---

<objective>
Unify renderer project identity to a single source of truth (useRiskReviewStore.projectId) and fix hook ordering violations in RiskResultPage.

Purpose: Eliminate the triple-store identity split (useProjectStore.selectedProjectId, useRiskReviewStore.projectId, useAppStore.taskId) that causes fragile state coordination. Fix React hook ordering to prevent conditional hook execution.

Output: Four files updated — processing page reads from correct store, result page has correct hook ordering, App.tsx cleaned of taskId usage for risk flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-integration-hardening/02-CONTEXT.md

@apps/desktop/src/renderer/features/risk-review/risk-review-store.ts
@apps/desktop/src/renderer/features/projects/project-store.ts
@apps/desktop/src/renderer/stores/app-store.ts
@apps/desktop/src/renderer/features/risk-review/risk-result-queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate project identity — processing page + App.tsx (HARDEN-01, D-01)</name>
  <files>
    apps/desktop/src/renderer/features/projects/project-processing-page.tsx,
    apps/desktop/src/renderer/features/projects/project-processing-page.test.tsx,
    apps/desktop/src/renderer/app/App.tsx
  </files>
  <read_first>
    apps/desktop/src/renderer/features/projects/project-processing-page.tsx,
    apps/desktop/src/renderer/features/projects/project-processing-page.test.tsx,
    apps/desktop/src/renderer/app/App.tsx,
    apps/desktop/src/renderer/features/risk-review/risk-review-store.ts,
    apps/desktop/src/renderer/features/projects/project-store.ts,
    apps/desktop/src/renderer/stores/app-store.ts
  </read_first>
  <action>
**Per D-01: useRiskReviewStore.projectId is the single source of truth for project identity.**

**1. ProjectProcessingPage (project-processing-page.tsx):**

Replace the import and destructuring:
- Remove: `import { useProjectStore } from './project-store';`
- Add: `import { useRiskReviewStore } from '../risk-review/risk-review-store';`
- Line 29: Change `const { selectedProjectId, clearSelection } = useProjectStore();` to:
  `const { projectId, setProjectId } = useRiskReviewStore();`
- Replace all occurrences of `selectedProjectId` with `projectId` in the component body (approximately 12 occurrences: lines 31, 32, 46, 59, 93, 99, 124, 126, 136, 157)
- Line 130: Change `clearSelection()` to `setProjectId(null)` in the `handleConfirmDelete` callback
- Line 139: Change `clearSelection()` to `setProjectId(null)` in the `handleBack` callback
- Remove the `clearSelection` from the destructuring since it's no longer needed (only `projectId` and `setProjectId` are used)

**2. ProjectProcessingPage test (project-processing-page.test.tsx):**

- Remove: `import { useProjectStore } from './project-store';`
- Add: `import { useRiskReviewStore } from '../risk-review/risk-review-store';`
- Line 30: Change `useProjectStore.setState({ selectedProjectId: id });` to `useRiskReviewStore.setState({ projectId: id });`
- Lines 43-52: Change the `beforeEach` reset to use `useRiskReviewStore.setState({ projectId: null, activeTab: 'overview', selectedFindingId: null, filters: { riskLevels: new Set(), detectorTypes: new Set(), reviewStatuses: new Set(), filePair: null, searchText: '', showImportantOnly: false }, selectedFindingIds: new Set() })` — reset the full risk review store state
- Line 203: Change `expect(useProjectStore.getState().selectedProjectId).toBeNull()` to `expect(useRiskReviewStore.getState().projectId).toBeNull()`

**3. App.tsx:**

- Line 60: Remove `useAppStore.getState().startTask(projectId);` from `startRiskProject()`. The `startTask` call sets `taskId` in `useAppStore` which is unnecessary for the risk flow (per D-01). The view transition to `project-processing` is handled by the progress subscription — when the first `risk:progress` event arrives, the processing page will render because `projectId` is set in `useRiskReviewStore`.
- Actually, the view transition IS needed. Keep `useAppStore.getState().setView('project-processing')` instead of `startTask(projectId)`. Change line 60 to: `useAppStore.getState().setView('project-processing');`
  This avoids setting `taskId` while still transitioning the view.
  </action>
  <verify>
    <automated>cd "D:/Projects/Nova/nova-bidlens" && pnpm --filter @bidlens/desktop test -- --run project-processing-page 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - project-processing-page.tsx line containing `useRiskReviewStore` exists (not `useProjectStore`)
    - project-processing-page.tsx does NOT contain the string `selectedProjectId`
    - project-processing-page.tsx does NOT contain the string `useProjectStore`
    - project-processing-page.test.tsx contains `useRiskReviewStore.setState({ projectId:` (not `useProjectStore.setState({ selectedProjectId:`)
    - App.tsx `startRiskProject` function does NOT contain `useAppStore.getState().startTask`
    - App.tsx `startRiskProject` function contains `useAppStore.getState().setView('project-processing')`
    - App.tsx `startRiskProject` function contains `useRiskReviewStore.getState().setProjectId(projectId)`
  </acceptance_criteria>
  <done>ProjectProcessingPage reads projectId from useRiskReviewStore. App.tsx startRiskProject sets view directly without setting taskId. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hook ordering in RiskResultPage (HARDEN-04, D-04)</name>
  <files>
    apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx
  </files>
  <read_first>
    apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx,
    apps/desktop/src/renderer/features/risk-review/risk-result-queries.ts
  </read_first>
  <action>
**Per D-04: Fix hook ordering violations (hooks called after early returns).**

The current code has `useFindingCounts(project.findings)` at line 118, AFTER two conditional returns (lines 90-99 for loading, lines 102-113 for error). This violates React's rules of hooks.

**Fix:**

1. Add a constant before the component function:
```typescript
const EMPTY_FINDINGS: never[] = [];
```

2. Move `useFindingCounts` to BEFORE the early returns. After the `useRiskResultDetail` hook (line 26) and before the `handleBack` callback (line 28), add:
```typescript
const counts = useFindingCounts(project?.findings ?? EMPTY_FINDINGS);
```

3. Remove the original `const counts = useFindingCounts(project.findings);` at line 118 (inside the render section after the early returns).

4. The `counts` variable is now available in all code paths. In the loading/error states it will have the empty-finding counts (all zeros), which is correct since those states don't display counts.
  </action>
  <verify>
    <automated>cd "D:/Projects/Nova/nova-bidlens" && pnpm --filter @bidlens/desktop test -- --run risk-result-page 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - risk-result-page.tsx contains `const EMPTY_FINDINGS` constant definition
    - risk-result-page.tsx contains `const counts = useFindingCounts(project?.findings ?? EMPTY_FINDINGS)` BEFORE any early return statements
    - risk-result-page.tsx does NOT contain `useFindingCounts(project.findings)` after any `if (isLoading` or `if (error` block
    - grep for `useFindingCounts` in risk-result-page.tsx returns exactly 1 match (the moved call)
  </acceptance_criteria>
  <done>RiskResultPage calls useFindingCounts before any early returns. Hook ordering is correct per React rules.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -rn "useProjectStore" apps/desktop/src/renderer/features/projects/project-processing-page.tsx` returns 0 matches
- `grep -rn "useAppStore.getState().startTask" apps/desktop/src/renderer/app/App.tsx` returns 0 matches
- `grep -n "useFindingCounts" apps/desktop/src/renderer/features/risk-review/risk-result-page.tsx` shows the call before any early return
- `pnpm --filter @bidlens/desktop test` passes
</verification>

<success_criteria>
1. ProjectProcessingPage reads projectId from useRiskReviewStore (not useProjectStore.selectedProjectId)
2. RiskResultPage hook ordering is correct — all hooks called before any early returns
3. App.tsx startRiskProject does not set taskId in useAppStore
4. All existing tests pass with updated store references
</success_criteria>

<output>
After completion, create `.planning/phases/02-integration-hardening/02-01-SUMMARY.md`
</output>
