---
phase: 01-cleanup-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/services/risk-review-service.ts
autonomous: true
requirements: [CLEAN-04]

must_haves:
  truths:
    - "Engine fallback path (buildFindings) no longer exists in risk-review-service.ts"
    - "If engineManager is unavailable, the operation fails with a clear error instead of producing fake evidence"
    - "No file pair assessment or project risk fallback paths remain"
  artifacts:
    - path: "apps/desktop/src/main/services/risk-review-service.ts"
      provides: "Risk review service with engine as hard dependency"
      does_not_contain: "function buildFindings"
  key_links:
    - from: "apps/desktop/src/main/services/risk-review-service.ts"
      to: "EngineManager"
      via: "this.engineManager.riskAnalyzeWithAst()"
      pattern: "this\\.engineManager\\.riskAnalyzeWithAst"
---

<objective>
Remove the engine fallback path that produces untraceable evidence with fake node IDs.

Purpose: The fallback `buildFindings` function creates evidence with synthetic `node-N` IDs that cannot be traced back to source documents. This violates the core value of traceable risk evidence. The Rust engine becomes a hard dependency.
Output: Single focused change to risk-review-service.ts — fallback removed, engine required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup-bug-fixes/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove engine fallback path (CLEAN-04, D-01)</name>
  <files>apps/desktop/src/main/services/risk-review-service.ts</files>
  <read_first>
    - apps/desktop/src/main/services/risk-review-service.ts (full file — focus on: engineManager field at line 70, the if/else at line 398/527, file pair fallback at line 581, project risk fallback at line 631, buildFindings at line 923, normalize at line 921, blockText at line 914)
  </read_first>
  <action>
**Per D-01: Completely remove the fallback. Engine is a hard dependency.**

1. **Change `engineManager` field type** (line 70): From `private readonly engineManager?: EngineManager` to `private readonly engineManager: EngineManager`. Remove the `?` optional marker. This makes the engine required at construction time.

2. **Remove the `else` fallback branch** (lines 527-531): Delete the entire `else { ... buildFindings(...) }` block. The `if (this.engineManager)` check becomes unnecessary since `engineManager` is now required — but keep the code inside the `if` block (the real pipeline). You can either:
   - Remove the `if (this.engineManager) {` wrapper and un-indent the real pipeline code (cleaner), OR
   - Keep the `if` check and add `else { throw new Error('分析引擎不可用'); }` (safer, D-01 says "if it fails to start, the operation fails with a clear error")
   
   Recommended: Keep the `if` check with an `else throw` — this handles the edge case where `engineManager` is set but becomes null at runtime.

3. **Remove file pair assessment fallback** (lines 581-612): The `else { // Fallback: compute locally from findings ... }` block after `if (filePairResults.length > 0)` is dead code when the engine always returns results. Delete the entire `else` branch. Keep only the `if (filePairResults.length > 0)` branch, and after it add: `else { throw new Error('引擎未返回文件对评估结果'); }`.

4. **Remove project risk fallback** (lines 631-655 area): The `else { // Fallback: compute locally ... }` block after the project risk persistence. Delete the `else` branch. Keep only the engine-computed path, and add `else { throw new Error('引擎未返回项目风险评估'); }`.

5. **Delete helper functions** that are only used by the fallback:
   - `buildFindings` function (lines 923-951) — only called from the deleted fallback
   - `normalize` function (line 921) — only called from `buildFindings`
   - `blockText` function (lines 914-918) — only called from `buildFindings`

6. **Update project creation metadata** (line 120): Change `modelVersion: 'lexical-fallback'` to `modelVersion: 'rust-engine'` since the fallback no longer exists.

7. **Check constructor call sites:** Search for where `RiskReviewService` is instantiated. The constructor now requires `engineManager` (not optional). If any call site passes `undefined` or omits it, that's a compile error — fix by ensuring the engine manager is always provided. Search: `new RiskReviewService(` in the codebase.
  </action>
  <verify>
    <automated>cd /d/Projects/Nova/nova-bidlens && pnpm --filter @bidlens/desktop test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/main/services/risk-review-service.ts` does NOT contain `function buildFindings`
    - `apps/desktop/src/main/services/risk-review-service.ts` does NOT contain `function normalize` (the standalone helper, not other normalize calls)
    - `apps/desktop/src/main/services/risk-review-service.ts` does NOT contain `function blockText`
    - `apps/desktop/src/main/services/risk-review-service.ts` does NOT contain `lexical-fallback`
    - `apps/desktop/src/main/services/risk-review-service.ts` contains `engineManager: EngineManager` (without `?`)
    - `apps/desktop/src/main/services/risk-review-service.ts` contains an error throw when engine is unavailable (e.g., `throw new Error` in else branch)
    - `pnpm --filter @bidlens/desktop test` exits 0 (or TypeScript compilation succeeds)
  </acceptance_criteria>
  <done>Engine fallback path fully removed. buildFindings and its helpers deleted. Engine is a required dependency — if unavailable, operation fails with clear error message instead of producing fake evidence.</done>
</task>

</tasks>

<verification>
After task completion:
1. `pnpm --filter @bidlens/desktop test` — tests pass
2. `grep -c "buildFindings\|lexical-fallback" apps/desktop/src/main/services/risk-review-service.ts` returns 0
3. `grep "engineManager: EngineManager" apps/desktop/src/main/services/risk-review-service.ts` confirms non-optional field
</verification>

<success_criteria>
- buildFindings function and its helpers (normalize, blockText) fully deleted
- No fallback path produces evidence with fake node IDs
- Engine is a hard dependency — clear error if unavailable
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-bug-fixes/01-02-SUMMARY.md`
</output>
