---
phase: 11-e2e-verification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/mixed-format-risk.test.ts
autonomous: true
requirements: [E2E-03]
gap_closure: true
must_haves:
  truths:
    - "混合格式项目（DOCX + PDF）经 Rust 引擎处理后产出跨格式 file-pair assessment"
    - "filePairAssessment 的 symmetricSimilarity 在 [0, 1] 范围内"
    - "跨格式 evidence 引用正确的 submissionId（当 findings 存在时）"
  artifacts:
    - path: "tests/integration/mixed-format-risk.test.ts"
      provides: "混合格式项目 E2E 集成测试（已修复）"
      contains: "Mixed Format Risk Detection"
  key_links:
    - from: "fileHash (valid hex)"
      to: "generate_node_id"
      via: "hex::decode → unique node IDs per submission"
      pattern: "fileHash"
    - from: "detectorRuns"
      to: "verification completeness"
      via: "确认所有检测器已执行"
      pattern: "detectorRuns"
---

<objective>
修复 mixed-format-risk.test.ts 中的 fileHash 问题并增强验证覆盖。

Purpose: 混合格式测试的 fileHash 同样使用了非法 hex 字符串。虽然测试目前通过（因为不同内容的 filePairAssessment 不依赖 node ID 唯一性），但应修复以保持一致性，并增加 detectorRuns 验证以确认检测器实际执行。

Output: 修复后的测试文件，验证更完整。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-verification/11-CONTEXT.md
@.planning/phases/11-e2e-verification/11-VERIFICATION.md

<!-- Key context: mixed-format test already passes, but fileHash is invalid hex -->
<context_note>
mixed-format-risk.test.ts 的 fileHash 使用 `'docx-sha256-placeholder'` 和 `'pdf-sha256-placeholder'`——都不是合法 hex。
测试通过是因为 DOCX 和 PDF 内容不同，filePairAssessment 基于 candidate pairs 而非 node ID 匹配。
但 evidence 条件检查（`if (findings.length > 0)`）意味着跨格式 evidence 从未被验证。
修复 fileHash 后，如果两个 submission 有相似文本段落，可能产出 findings。
</context_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 fileHash 并增强验证覆盖</name>
  <files>tests/integration/mixed-format-risk.test.ts</files>
  <action>
修复 `tests/integration/mixed-format-risk.test.ts` 中的 fileHash 并增强验证。

**修改内容：**

1. 修复 `buildDocxAst` 中的 sha256：
   ```typescript
   // 从
   sha256: 'docx-sha256-placeholder',
   // 改为
   sha256: 'd'.repeat(64), // 合法 hex
   ```

2. 修复 `buildPdfAst` 中的 sha256：
   ```typescript
   // 从
   sha256: 'pdf-sha256-placeholder',
   // 改为
   sha256: 'e'.repeat(64), // 合法 hex
   ```

3. 修复测试中 submission 的 fileHash 引用（多处）：
   - `'Rust engine processes mixed-format project'` 测试中的 `fileHash: docxAst.sha256` 和 `fileHash: pdfAst.sha256` — 这些引用 DocumentAst 的 sha256 字段，修复 #1 和 #2 后自动生效
   - `'cross-format project risk assessment is computed'` 测试中同理

4. 在 `'Rust engine processes mixed-format project'` 测试中增加 detectorRuns 验证：
   ```typescript
   // 验证检测器执行记录
   expect(result.detectorRuns).toBeDefined();
   expect(result.detectorRuns.length).toBeGreaterThan(0);
   for (const run of result.detectorRuns) {
     expect(['completed', 'skipped']).toContain(run.status);
   }
   ```

5. 将 evidence 条件检查改为显式注释说明：
   ```typescript
   // 不同内容的 submission 通常不会产出 findings（无相似文本段落）
   // findings.length === 0 是正常的——这里验证的是 filePairAssessment，不是 findings
   if (result.findings.length > 0) {
     // 如果有 findings，验证 evidence 引用正确
     for (const finding of result.findings) {
       expect(finding.involvedSubmissionIds.some(
         id => id === 'sub-docx' || id === 'sub-pdf',
       )).toBe(true);
       // ... existing evidence checks
     }
   }
   ```

**不要改动：** DOCX/PDF AST 构建逻辑，引擎通信模式，toEngineDocumentAst 调用。
  </action>
  <verify>
    <automated>cd D:/Projects/Nova/nova-bidlens && pnpm vitest run tests/integration/mixed-format-risk.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>fileHash 修复为合法 hex，detectorRuns 验证通过，evidence 条件检查有明确注释说明</done>
</task>

</tasks>

<verification>
修复后验证：
1. `pnpm vitest run tests/integration/mixed-format-risk.test.ts` 全部通过
2. `pnpm vitest run tests/integration/` 全部通过
</verification>

<success_criteria>
- mixed-format-risk.test.ts 全部通过
- fileHash 使用合法 64 字符 hex
- detectorRuns 验证存在
- evidence 条件检查有注释说明为什么 findings 可能为 0
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-verification/11-04-SUMMARY.md`
</output>
