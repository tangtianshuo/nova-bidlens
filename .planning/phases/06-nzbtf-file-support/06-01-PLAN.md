---
phase: 06-nzbtf-file-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/risk-review.ts
  - apps/desktop/src/renderer/features/projects/submission-file-list.tsx
  - apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx
  - apps/desktop/src/main/ipc/compare-handlers.ts
  - apps/desktop/src/main/services/risk-review-service.ts
autonomous: true
requirements:
  - NZBTF-01
  - NZBTF-03

must_haves:
  truths:
    - "User can select .nzbtf files in the project creation dialog"
    - "nzbtf is a valid RiskFileFormat throughout the type system"
    - "File validator accepts .nzbtf files as supported"
  artifacts:
    - path: "packages/shared/src/risk-review.ts"
      provides: "RiskFileFormat includes 'nzbtf'"
      contains: "'docx' | 'pdf' | 'nzbtf'"
    - path: "apps/desktop/src/renderer/features/projects/submission-file-list.tsx"
      provides: "UI accepts .nzbtf files"
      contains: "nzbtf"
    - path: "apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx"
      provides: "Baseline slot accepts .nzbtf"
      contains: "nzbtf"
  key_links:
    - from: "packages/shared/src/risk-review.ts"
      to: "apps/desktop/src/renderer/features/projects/submission-file-list.tsx"
      via: "RiskFileFormat type import"
      pattern: "RiskFileFormat"
    - from: "apps/desktop/src/main/services/risk-review-service.ts"
      to: "packages/shared/src/risk-review.ts"
      via: "fileFormat cast"
      pattern: "as.*docx.*pdf.*nzbtf"
---

<objective>
Extend the type system and UI to accept .nzbtf files alongside .docx and .pdf.

Purpose: nZBTF files must flow through the entire file selection, validation, and submission creation pipeline. This plan adds 'nzbtf' to RiskFileFormat and updates all format references in UI and IPC layers.
Output: Type-safe nzbtf format support across shared types, renderer UI, and main process IPC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-nzbtf-file-support/06-CONTEXT.md
@.planning/phases/06-nzbtf-file-support/06-RESEARCH.md

<interfaces>
<!-- Key types the executor must know about -->

From packages/shared/src/risk-review.ts (line 22):
```typescript
export type RiskFileFormat = 'docx' | 'pdf';
```

From apps/desktop/src/renderer/features/projects/submission-file-list.tsx (line 13):
```typescript
format: 'docx' | 'pdf';
```

From apps/desktop/src/renderer/features/projects/submission-file-list.tsx (line 42):
```typescript
const ALLOWED_FORMATS: Array<'docx' | 'pdf'> = ['docx', 'pdf'];
```

From apps/desktop/src/main/services/risk-review-service.ts (line 131):
```typescript
fileFormat: path.extname(file.path).slice(1).toLowerCase() as 'docx' | 'pdf',
```

From apps/desktop/src/main/services/risk-review-service.ts (line 693):
```typescript
fileFormat: s.file_format as 'docx' | 'pdf',
```

From apps/desktop/src/main/services/risk-review-service.ts (line 866):
```typescript
fileFormat: row.file_format as 'docx' | 'pdf',
```

From apps/desktop/src/main/ipc/compare-handlers.ts (line 109-111):
```typescript
filters: [
  { name: 'Word 文档', extensions: ['docx'] },
  { name: '所有文件', extensions: ['*'] },
],
```

From apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx (line 26):
```typescript
const ACCEPT_DEFAULT = '.docx,.pdf';
```

From apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx (line 157):
```typescript
支持 .docx, .pdf
```

From apps/desktop/src/main/services/file-validator.ts (line 163):
```typescript
const parser = globalRegistry.findByExtension(ext);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'nzbtf' to RiskFileFormat and update all type cast sites</name>
  <files>
    packages/shared/src/risk-review.ts,
    apps/desktop/src/main/services/risk-review-service.ts
  </files>
  <read_first>
    packages/shared/src/risk-review.ts,
    apps/desktop/src/main/services/risk-review-service.ts
  </read_first>
  <action>
1. In `packages/shared/src/risk-review.ts` line 22, change:
   `export type RiskFileFormat = 'docx' | 'pdf';`
   to:
   `export type RiskFileFormat = 'docx' | 'pdf' | 'nzbtf';`

2. In `apps/desktop/src/main/services/risk-review-service.ts`, update ALL type cast sites to include `'nzbtf'`:
   - Line 131: `as 'docx' | 'pdf'` -> `as 'docx' | 'pdf' | 'nzbtf'`
   - Line 693: `as 'docx' | 'pdf'` -> `as 'docx' | 'pdf' | 'nzbtf'`
   - Line 866: `as 'docx' | 'pdf'` -> `as 'docx' | 'pdf' | 'nzbtf'`

   Also in `cacheDocumentAst` (around line 648), update the hardcoded `fileFormat: 'docx'` to use a dynamic value. Change:
   `fileFormat: 'docx',`
   to:
   `fileFormat: (ast.filename.split('.').pop()?.toLowerCase() === 'nzbtf' ? 'nzbtf' : 'docx') as RiskFileFormat,`
   Import `RiskFileFormat` from `@bidlens/shared` if not already imported.
  </action>
  <verify>
    <automated>cd "D:/Projects/Nova/nova-bidlens" && pnpm --filter @bidlens/shared build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/risk-review.ts` contains `'docx' | 'pdf' | 'nzbtf'`
    - `apps/desktop/src/main/services/risk-review-service.ts` contains no remaining `as 'docx' | 'pdf'` casts (all updated to include 'nzbtf')
    - `pnpm --filter @bidlens/shared build` exits 0
  </acceptance_criteria>
  <done>RiskFileFormat type includes 'nzbtf' and all cast sites in risk-review-service.ts are updated</done>
</task>

<task type="auto">
  <name>Task 2: Update UI components to accept .nzbtf files</name>
  <files>
    apps/desktop/src/renderer/features/projects/submission-file-list.tsx,
    apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx,
    apps/desktop/src/main/ipc/compare-handlers.ts
  </files>
  <read_first>
    apps/desktop/src/renderer/features/projects/submission-file-list.tsx,
    apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx,
    apps/desktop/src/main/ipc/compare-handlers.ts
  </read_first>
  <action>
1. In `apps/desktop/src/renderer/features/projects/submission-file-list.tsx`:
   - Line 13: Change `format: 'docx' | 'pdf';` to `format: 'docx' | 'pdf' | 'nzbtf';`
   - Line 42: Change `const ALLOWED_FORMATS: Array<'docx' | 'pdf'> = ['docx', 'pdf'];` to `const ALLOWED_FORMATS: Array<'docx' | 'pdf' | 'nzbtf'> = ['docx', 'pdf', 'nzbtf'];`
   - Line 199: Change `if (ext !== 'docx' && ext !== 'pdf') continue;` to `if (ext !== 'docx' && ext !== 'pdf' && ext !== 'nzbtf') continue;`
   - Line 203: Change `const format = ext as 'docx' | 'pdf';` to `const format = ext as 'docx' | 'pdf' | 'nzbtf';`
   - Line 276: Change the hint text from `支持 .docx 和文字版 .pdf，{minFiles}-{maxFiles} 个文件` to `支持 .docx、.pdf 和 .nzbtf，{minFiles}-{maxFiles} 个文件`
   - Line 310: Change `accept=".docx,.pdf"` to `accept=".docx,.pdf,.nzbtf"`
   - Line 99: Update error message from `不支持的格式（仅支持 .docx 和文字版 .pdf）` to `不支持的格式（仅支持 .docx、.pdf 和 .nzbtf）`

2. In `apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx`:
   - Line 26: Change `const ACCEPT_DEFAULT = '.docx,.pdf';` to `const ACCEPT_DEFAULT = '.docx,.pdf,.nzbtf';`
   - Line 157: Change `支持 .docx, .pdf` to `支持 .docx, .pdf, .nzbtf`

3. In `apps/desktop/src/main/ipc/compare-handlers.ts`:
   - Around line 109-111, add nzbtf to the file dialog filters. After the existing filters array, add:
     `{ name: 'nZBTF 投标文件', extensions: ['nzbtf'] },`
     So the filters become:
     ```
     { name: 'Word 文档', extensions: ['docx'] },
     { name: 'nZBTF 投标文件', extensions: ['nzbtf'] },
     { name: '所有文件', extensions: ['*'] },
     ```
  </action>
  <verify>
    <automated>cd "D:/Projects/Nova/nova-bidlens" && pnpm --filter @bidlens/desktop build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/renderer/features/projects/submission-file-list.tsx` contains `format: 'docx' | 'pdf' | 'nzbtf'`
    - `apps/desktop/src/renderer/features/projects/submission-file-list.tsx` contains `accept=".docx,.pdf,.nzbtf"`
    - `apps/desktop/src/renderer/features/projects/submission-file-list.tsx` contains `支持 .docx、.pdf 和 .nzbtf`
    - `apps/desktop/src/renderer/features/projects/tender-baseline-slot.tsx` contains `ACCEPT_DEFAULT = '.docx,.pdf,.nzbtf'`
    - `apps/desktop/src/main/ipc/compare-handlers.ts` contains `'nZBTF 投标文件'`
    - `pnpm --filter @bidlens/desktop build` exits 0
  </acceptance_criteria>
  <done>UI file selection dialogs and validation accept .nzbtf files alongside .docx and .pdf</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @bidlens/shared build` succeeds
2. `pnpm --filter @bidlens/desktop build` succeeds
3. `grep -rn "nzbtf" packages/shared/src/risk-review.ts` returns the updated type
4. `grep -rn "nzbtf" apps/desktop/src/renderer/features/projects/submission-file-list.tsx` returns matches in format type, ALLOWED_FORMATS, accept attribute, and hint text
</verification>

<success_criteria>
- RiskFileFormat = 'docx' | 'pdf' | 'nzbtf' in shared types
- All UI components accept .nzbtf files
- File dialog includes nZBTF filter option
- TypeScript build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-nzbtf-file-support/06-01-SUMMARY.md`
</output>
